@using BookQuotes.Domain.ValueObjects

<MudGrid>
    <MudItem xs="12">
        <MudStack Row="true" Spacing="6">
            <MudFileUpload T="IReadOnlyList<IBrowserFile>" FilesChanged="UploadFiles"
                           Accept=".epub,.xml,.annot" AppendMultipleFiles="false">
                <CustomContent>
                    <MudButton Variant="Variant.Filled"
                               Color="Color.Primary"
                               OnClick="@context.OpenFilePickerAsync"
                               StartIcon="@Icons.Material.Filled.CloudUpload">
                        Upload Epub or XML files with annotations
                    </MudButton>
                </CustomContent>
                <SelectedTemplate />
            </MudFileUpload>
            <MudToggleGroup T="BookSearchMode"
                            @bind-Value="SearchMode"
                            SelectionMode="SelectionMode.SingleSelection"
                            Color="Color.Primary">
                <MudToggleItem Value="BookSearchMode.None">
                    No search
                </MudToggleItem>
                <MudToggleItem Value="BookSearchMode.French">
                    French search
                </MudToggleItem>
                <MudToggleItem Value="BookSearchMode.English">
                    English search
                </MudToggleItem>
            </MudToggleGroup>
            <MudSpacer />
            <MudButton Variant="Variant.Filled" Color="Color.Secondary"
                       Disabled="Books.Count == 0"
                       OnClick="Reset">Reset</MudButton>
        </MudStack>
    </MudItem>
    <MudItem xs="12">
        <MudStack Spacing="4">
        @foreach (var book in Books)
        {
            var epubStream = EpubStreams.TryGetValue(book.Title, out var stream) ? stream : null;
            <BookComponent Book="@book" EpubStream="@epubStream"></BookComponent>
        }
        </MudStack>
    </MudItem>
</MudGrid>
